
// Example: Add alert for buttons (optional)
document.querySelectorAll('.button').forEach(button => {
    button.addEventListener('click', () => {
        alert("Redirecting to the respective page...");
    });
});
